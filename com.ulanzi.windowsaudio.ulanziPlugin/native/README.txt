This folder must contain ulanzi-audio-helper.exe on Windows.

Build it from the repository native/ directory (see native/README.md) and copy
the Release x64 binary here before installing the plugin into Ulanzi Studio.

The helper uses a local named pipe only. It does not open TCP ports.
